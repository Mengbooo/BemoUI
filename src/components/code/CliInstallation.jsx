import { Box, Text } from "@chakra-ui/react";

const CliInstallation = () => (
  <Box>
    <h2 className="demo-title">CLI</h2>
    <Text mb={2} mt={4} color="#a1a1aa">
      CLI tools are not available at this time (WIP) . Please copy the code and paste it into your project.
    </Text>
  </Box>
);
export default CliInstallation;
